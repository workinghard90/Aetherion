18.20.3

# Allow installing packages with unmet peer dependencies (fixes many RN issues)
legacy-peer-deps=true

# Avoid audit warnings failing the build (Netlify sometimes fails on high-severity issues)
audit=false

# Set default registry to ensure consistency
registry=https://registry.npmjs.org/

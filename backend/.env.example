# Copy this file to `.env` and fill in real values

# PostgreSQL URL (or keep using SQLite)
DATABASE_URL=sqlite:///universe.db

# Where uploaded vault files are stored
UPLOAD_FOLDER=uploads

# Secret key for JWT or session
SECRET_KEY=Autumn&Caelum

# PORT for production (Render will set automatically)
PORT=5000
